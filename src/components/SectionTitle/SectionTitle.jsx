import React from 'react';

const SectionTitle = ({title}) => {
    return (
        <div>
            <h1 className='text-2xl font-bold text-center p-5'>{title}</h1>
        </div>
    );
};

export default SectionTitle;